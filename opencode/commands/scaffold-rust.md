---
description: Scaffold a new Rust project with Nix flake at the given path
---
Scaffold a new Rust project at the path: $ARGUMENTS

Run the scaffold pipeline and report the result:
!`bun run --cwd $AI_CODING_MONOREPO pipeline scaffold-rust $ARGUMENTS 2>&1`

Summarise what files were created. If the pipeline failed, explain the error clearly.
